Place release APKs here as `teknofest-yatay-latest.apk`.

Generated by `tool/publish_apk.ps1`. Binary APK files are gitignored so a
deploy without a new APK does not wipe the live file (see `tool/deploy.ps1`).

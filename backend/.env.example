# Runtime (application) — never put dev_admin here.
PG_HOST=10.6.228.154
PG_PORT=5432
PG_DATABASE=teknofestKiosk
PG_USER=appuser
PG_PASSWORD=

APP_ENV=test
APP_VERSION=1.0.0
PG_POOL_SIZE=8

# Admin / migration only. Used by scripts/migrate.py and scripts/seed.py.
# Do not export these into the API process.
PG_ADMIN_USER=dev_admin
PG_ADMIN_PASSWORD=
